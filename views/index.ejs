<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Food Voting App</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <% if (user) { %>
        <p>Welcome, <%= user.username %>! <a href="/logout">Logout</a></p>
        <a href="/add-food">Add New Food</a> | 
        <a href="/results">View Results</a>
    <% } else { %>
        <p><a href="/login">Login</a> to add foods or view results</p>
    <% } %>
    <h1>Food Voting App</h1>
    
    <h2>Vote for a Food</h2>
    <form action="/vote" method="POST">
        <label for="foodName">Select Food:</label>
        <select id="foodName" name="foodName" required>
            <option value="">Choose a food</option>
            <% foods.forEach(food => { %>
                <option value="<%= food.name %>"><%= food.name %></option>
            <% }); %>
        </select>
        <br><br>
        <label for="taste">Taste (1-10):</label>
        <input type="number" id="taste" name="taste" min="1" max="10" required>
        <br><br>
        <label for="look">Look (1-10):</label>
        <input type="number" id="look" name="look" min="1" max="10" required>
        <br><br>
        <label for="feel">Feel (1-10):</label>
        <input type="number" id="feel" name="feel" min="1" max="10" required>
        <br><br>
        <button type="submit">Submit Vote</button>
    </form>
    <br>
    <a href="/add-food">Add New Food</a>
    <br>
    <a href="/results">View Results</a>
</body>
</html>